# 관용 표현 학습 웹앱 인수인계 자료

## 1. 바로 전달할 안내문

아래 내용을 일반 웹 버전 GPT의 새 대화에 붙여 넣고, 이어서 프로젝트 파일을 업로드합니다.

```text
당신은 초등학교 6학년 국어 수업용 웹앱을 함께 개선하는 개발 협력자입니다.

프로젝트 목적은 학생이 관용 표현의 뜻을 문맥 속에서 고르고, 교사가 학생별·문항별 결과를 확인하는 학습 웹앱을 만드는 것입니다. 모든 답변, 화면 문구, 코드 주석은 한국어로 작성해 주세요.

가장 먼저 업로드한 인수인계 자료와 README.md, PROJECT_PLAN.md, DESIGN.md, TEST_REPORT.md를 읽고 기존 구조를 분석해 주세요. 그다음에는 수정할 파일과 이유를 먼저 짧게 제시하고 작업하세요.

현재 웹앱의 핵심 규칙은 다음과 같습니다.
- 이름, 연락처 등 개인정보를 수집하지 않고 학생 번호만 사용합니다.
- 학생 번호가 이미 저장되어 있으면 “이미 n번의 기록이 있습니다. 다시 진행하시겠습니까?”라고 묻고, 취소하면 활동을 시작하지 않습니다.
- 한 화면에 한 문항만 제시합니다.
- 선택지는 매 활동마다 무작위로 섞이며, 화면에 보이는 순서대로 1~4번 번호를 표시합니다.
- 첫 선택의 정오와 첫 선택까지의 시간을 기록합니다.
- 첫 선택이 오답이어도 정답을 찾을 때까지 다시 고를 수 있습니다.
- 정답을 찾아야 다음 문항으로 갈 수 있습니다.
- 학생 결과와 교사 분석은 현재 브라우저의 localStorage를 사용합니다.
- Firebase 연동은 실제 프로젝트 생성·인증 설정·데이터베이스 생성·규칙 배포·Hosting 배포 전에 반드시 사용자 승인을 받아야 합니다.
- 비밀번호, API 비밀키, 인증 토큰, 서비스 계정 키, 실제 학생 개인정보를 코드나 예시에 넣지 마세요.
- 삭제, 초기화, 기존 데이터 덮어쓰기, 배포는 반드시 사용자 승인을 받아야 합니다.

디자인 방향은 밝고 따뜻한 시작 화면 + 어두운 보라·남색의 집중형 문항 화면입니다. 선택지는 큰 번호 원형, 보라색 포커스 테두리, 가볍게 앞으로 이동하는 반응 효과를 사용합니다. 초등학교 6학년이 읽기 쉬운 대비와 글자 크기를 유지하세요.

특히 문제 은행을 100문항까지 확장할 때에는 다음을 지켜 주세요.
- 초등학교 6학년 국어의 관용 표현 학습 수준에 맞는 표현만 사용합니다.
- 정답과 오답의 문장 길이가 한쪽으로 치우치지 않게 합니다.
- 오답도 그럴듯한 길이와 설명을 갖되, 문맥상 맞지 않는 의미가 되게 합니다.
- 실제 학생 데이터 대신 가상 번호만 사용합니다.

작업 후에는 변경 파일, 구현 기능, 실행한 테스트, 테스트 결과, 남은 문제를 한국어로 간단히 보고해 주세요.
```

## 2. 업로드할 파일 순서

일반 웹 버전 GPT에는 다음 파일을 함께 올립니다. 압축 파일 하나를 올릴 수 있다면 `관용표현학습앱_개선본.zip`을 우선 사용합니다.

1. `GPT_웹버전_인수인계.md`
2. `README.md`
3. `PROJECT_PLAN.md`
4. `DESIGN.md`
5. `TEST_REPORT.md`
6. `index.html`
7. `css/style.css`
8. `js/app.js`
9. `js/questions.js`
10. `js/analysis.js`
11. 필요할 때만 `firebase.json`, `public` 폴더, `package.json`

## 3. 현재 완성된 기능

| 영역 | 현재 상태 |
| --- | --- |
| 학생 시작 | 학생 번호 1~4자리 입력과 기존 기록 확인을 지원함 |
| 문제 풀이 | 5문항, 한 화면 한 문항, 선택지 무작위 배치, 1~4번 표시를 지원함 |
| 응답 기록 | 첫 응답 정오·시간을 기록하고 오답 후 재도전을 허용함 |
| 학생 결과 | 점수, 첫 응답 정답률, 평균 시간을 표시함 |
| 교사 분석 | 참여 학생 수, 문항별 정답률·평균·중앙값 시간·선택지 분포·취약 문항을 표시함 |
| 결과 활용 | 인쇄, CSV 다운로드, 표 복사, CSV 텍스트 복사를 지원함 |
| 데이터 저장 | 현재 localStorage 기반으로 여러 브라우저 간 공유는 아직 불가함 |
| Firebase | 코드 구조와 설정 초안이 있으나 실제 연결·배포는 하지 않음 |

## 4. 파일별 역할

| 파일 | 역할 |
| --- | --- |
| `index.html` | 학생 시작·문항·결과·교사 분석 화면의 구조 |
| `css/style.css` | 전체 디자인, 반응형 화면, 선택지 애니메이션 |
| `js/questions.js` | 관용 표현 문항과 선택지 데이터, 선택지 섞기 함수 |
| `js/app.js` | 학생 활동 진행, 첫 응답 기록, 재도전, 결과 저장 |
| `js/analysis.js` | 교사 통계, CSV·복사·인쇄 기능 |
| `README.md` | 실행과 Firebase 연결 안내 |
| `PROJECT_PLAN.md` | 구현 목표와 이후 작업 계획 |
| `DESIGN.md` | 색상·글꼴·여백·화면 디자인 원칙 |
| `TEST_REPORT.md` | 가상 학생 10회 반복 테스트 결과 |
| `firebase.json` 및 `public` | 초기 Firebase Hosting/Firestore용 구조. 현재 루트 웹앱과 별도이므로 통합 전 검토 필요 |

## 5. 데이터 흐름

```text
학생 번호 입력
  → 기존 번호 기록 확인
  → 문항과 선택지 순서 생성
  → 첫 선택 시 정오·응답 시간 저장
  → 오답이면 재선택, 정답이면 다음 문항
  → 활동 종료 시 localStorage에 학생별 결과 저장
  → 교사 화면에서 전체 결과를 통계로 변환
```

현재 localStorage 키는 `idiomQuizResultsV2`입니다. 문항 내용이 달라졌으므로 이전 결과와 섞이지 않도록 구분한 키입니다.

## 6. 반드시 유지할 중요 규칙

- 시작 화면에서 기존 학생 번호 확인을 거치지 않고 바로 문제로 넘어가면 안 됩니다.
- 첫 응답 이후의 재선택은 학습용이며, 첫 응답 정답률과 시간 통계는 바꾸면 안 됩니다.
- 선택지 번호는 원래 문항 데이터의 번호가 아니라, 무작위로 섞여 현재 화면에 보이는 순서여야 합니다.
- 정답을 맞히기 전에는 다음 문항 버튼을 보이지 않게 합니다.
- 문제 은행 확장 시 학생이 ‘정답 설명이 더 길어서’ 답을 고르는 단서를 만들면 안 됩니다.
- Firebase 구성값은 예시 파일 또는 환경 설정으로 분리하고, 민감 정보는 기록하지 않습니다.

## 7. 다음 우선 작업 제안

1. 5문항 데이터 구조를 유지한 채 100문항 문제 은행으로 확장합니다.
2. 한 활동에 낼 문항 수와 중복 방지 방식을 결정합니다.
3. localStorage 결과 형식을 유지하면서 Firestore 저장 어댑터를 추가합니다.
4. 교사 로그인과 Firestore Security Rules를 실제 환경에 연결합니다. 이 단계부터는 Firebase 프로젝트 선택·인증 설정·배포 전에 사용자 승인이 필요합니다.
5. 실제 기기와 브라우저에서 학생 화면, 교사 화면, 인쇄 화면을 다시 점검합니다.

## 8. 알려진 확인 사항

- 가상 학생 10명으로 정답·오답·재도전·기록 덮어쓰기·선택지 무작위 배치·교사 통계를 확인했습니다.
- 현재 개발 환경에서는 자동 브라우저 화면 검사를 끝까지 실행할 수 없었으므로, 파일을 열어 실제 화면에서 색상·애니메이션·인쇄를 한 번 확인하는 것이 좋습니다.
- `public` 폴더의 초기 Firebase용 코드와 현재 루트의 정적 웹앱은 역할이 겹칠 수 있습니다. Firebase 연동 단계에서 하나의 구조로 정리하되, 기존 파일 삭제 전에는 승인받아야 합니다.

## 9. 웹 버전 GPT에 요청하는 예시

```text
첨부한 프로젝트를 분석한 뒤, 5문항 구조와 첫 응답 기록 규칙을 유지하면서 관용 표현 문제 은행을 100문항으로 확장해 주세요. 각 문항은 초등학교 6학년 수준인지 점검하고, 네 선택지의 길이가 지나치게 차이 나지 않도록 작성해 주세요. 먼저 수정 계획과 대상 파일을 알려 주고, 그다음 변경 내용을 제시해 주세요.
```


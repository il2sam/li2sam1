{
  "name": "grade5-korean-word-choice",
  "version": "1.0.0",
  "private": true,
  "description": "초등학교 5학년 국어 내용 이해 단어 선택 활동",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:rules": "firebase emulators:exec --only firestore \"node --test tests/firestore.rules.test.mjs\"",
    "check": "node --check public/js/activity-data.js && node --check public/js/quiz-engine.js && node --check public/js/analytics.js && node --check public/js/export-utils.js && node --check public/js/firebase-client.js && node --check public/js/student-app.js && node --check public/js/teacher-app.js",
    "serve": "firebase emulators:start --only hosting",
    "emulators": "firebase emulators:start --only auth,firestore,hosting"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.10.0",
    "firebase-tools": "^14.10.1"
  }
}

